# 서울 전월세 실거래가 탐색기

조건 입력 → 목록 출력 웹앱. 국토교통부 아파트 전월세 실거래가(공공데이터포털)를
합법적으로 활용해, 서울 25개 자치구의 실거래 기록을 조건에 맞춰 탐색한다.

**전체 설계는 `DESIGN.md`에 있다. 작업 전 반드시 먼저 읽을 것.**

---

## 작업 원칙

1. **설계 우선.** 코드를 짜기 전에 `DESIGN.md`의 해당 섹션을 확인한다.
   설계와 어긋나는 구현이 필요하면 먼저 짚고 합의한 뒤 진행한다.

2. **의존성 순서대로(bottom-up) 구현.** 아래에서 위로:
   `districts.js` / `normalize.js` → `molit-client.js` → `routes.js` → `server.js`
   → 프론트(`index.html` / `styles.css` / `app.js`)

3. **한 번에 한 모듈.** 모듈 하나를 끝내고 검증한 뒤 다음으로 넘어간다.
   여러 파일을 한꺼번에 쏟아내지 않는다.

4. **순수함수는 테스트와 함께.** `normalize.js`와 프론트 `filterSort`는
   외부 의존이 없는 순수함수다. 구현 시 가짜 데이터로 단위 테스트를 같이 만든다.

---

## 절대 원칙 (설계의 핵심)

- **공공 API를 아는 모듈은 `molit-client.js` 하나뿐.** 다른 어떤 모듈도
  공공 API의 URL·파라미터·응답 형식을 직접 알아선 안 된다. (향후 유료 매물 API를
  형제 모듈로 붙이기 위한 핵심 확장 포인트.)

- **수집은 백엔드, 필터·정렬은 프론트.** `/api/search`는 필터 파라미터를 받지 않고
  "구 + 기간"만 받아 정규화된 원본 전체를 내려준다. 보증금·면적·정렬은 프론트가
  받아둔 데이터로 처리한다(재호출 없음).

- **인증키는 백엔드에만.** 브라우저에 절대 노출하지 않는다. `.env`로 관리하고
  `.gitignore`에 포함한다.

- **단일 데이터 계약 `RentDeal`.** 모든 모듈은 이 타입으로 소통한다.
  금액 단위는 만원, 면적은 전용면적 ㎡. 전세/월세는 `monthlyRent > 0`으로 판별.

---

## 스택

- 백엔드: Node.js + Express (의존성: express, dotenv)
- 프론트: 바닐라 HTML/CSS/JS (빌드 스텝 없음)
- 실행: `.env`에 공공데이터포털 인증키(`SERVICE_KEY`) 넣고 `npm start`

---

## 합법성 (타협 불가)

데이터 출처는 국토교통부 공식 공개 API(data.go.kr)로 한정한다.
네이버/직방 등 매물 플랫폼의 비공식 스크래핑은 ToS·법적 리스크로 제외한다.
이 원칙을 우회하는 구현은 제안하지 않는다.
