SERVICE_KEY=여기에_공공데이터포털_인증키_입력
PORT=3000
# 서울 열린데이터광장(data.seoul.go.kr) 인증키 — 중개업소 조회용 (없으면 해당 기능만 비활성)
SEOUL_API_KEY=
# "오늘의 집값은?" 시세 수집용 (없으면 해당 페이지만 비활성)
# Supabase 대시보드 → Settings → API → service_role 키 (절대 브라우저에 노출 금지)
SUPABASE_SERVICE_ROLE_KEY=
# 시세 수집 엔드포인트 보호용 임의 문자열 (Vercel Cron이 이 값으로 인증)
CRON_SECRET=
